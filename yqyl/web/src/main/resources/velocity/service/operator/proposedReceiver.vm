#set($currentPage="AUDIT_RECEIVER")
#parse("/velocity/service/operator/navigation.vm")

<link rel="stylesheet"
	href="/static/css/business/service/operator/proposedReceiver.css"></link>

<div class="proposedReceiver" ng-controller="proposedReceiverController">
	<table>
		<thead>
			<tr>
				<th>#label("ID")</th>
				<th>#label("YIJINCODE")</th>
				<th>#label("NAME")</th>
				<th>#label("IDENTITY")</th>
			</tr>
		</thead>
		<tbody>
			<tr ng-repeat="receiver in receivers">
				<td><a href="/service/operator/audit/receiver/{{receiver.id}}">{{receiver.id}}</a></td>
				<td>{{receiver.yijinCode}}</td>
				<td>{{receiver.name}}</td>
				<td>{{receiver.identityCard}}</td>
			</tr>
		</tbody>
	</table>
	<pagination total-items="paging.itemCount" ng-model="paging.pageIndex"
		max-size="10" items-per-page="paging.pageSize" class="pagination-sm"
		boundary-links="true" ng-change="pageChanged()"
		first-text='#label("FIRST_PAGE")' last-text='#label("LAST_PAGE")'
		previous-text='#label("PREVIOUS_PAGE")'
		next-text='#label("NEXT_PAGE")'></pagination>
</div>

<script src="/static/js/business/service/operator/proposedReceiver.js"></script>